# ── Core ─────────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...          # claude.ai → API Keys

# ── GitHub ───────────────────────────────────────────────────────────────────
GITHUB_TOKEN=ghp_...                  # github.com/settings/tokens → read:user, read:org, public_repo

# ── Email Discovery ──────────────────────────────────────────────────────────
HUNTER_API_KEY=...                    # hunter.io → free 25/mo

# ── Email Sending — Option A: Gmail OAuth (recommended) ──────────────────────
# Setup: console.cloud.google.com → Enable Gmail API → OAuth 2.0 Desktop App
# Download credentials.json → place at /Users/aitsgroup/networking-agent/credentials.json
# First run opens browser to grant access → token.json saved automatically

# ── Email Sending — Option B: Gmail SMTP App Password (simpler) ──────────────
GMAIL_ADDRESS=saraswatdas94@gmail.com
GMAIL_APP_PASSWORD=...                # myaccount.google.com/apppasswords (needs 2FA)

# ── X (Twitter) ──────────────────────────────────────────────────────────────
# Setup: developer.twitter.com → New App → Free tier → Keys & Tokens
X_API_KEY=...
X_API_SECRET=...
X_ACCESS_TOKEN=...
X_ACCESS_SECRET=...

# ── LinkedIn (browser automation) ────────────────────────────────────────────
LINKEDIN_EMAIL=saraswatdas94@gmail.com
LINKEDIN_PASSWORD=...                 # WARNING: use a burner or app password if possible

# ── Database ─────────────────────────────────────────────────────────────────
NETWORKING_DB=/Users/aitsgroup/networking-agent.db

# ── Optional: UK Companies House (free, 600 req/5min) ────────────────────────
UK_CH_API_KEY=...                     # find-and-update.company-information.service.gov.uk/get-started

# ── Optional: Google Maps (Track B lead sourcing) ────────────────────────────
# GOOGLE_MAPS_API_KEY=...             # console.cloud.google.com → Places API → $200 free credit/mo

# ── Optional: Crunchbase ─────────────────────────────────────────────────────
# CRUNCHBASE_API_KEY=...              # crunchbase.com → free tier 200 req/mo
